decl
	integer swap(string &x,&y);
enddecl
integer swap(string &x,&y)
{
decl
	string a;
enddecl
begin
	a=x;
	x=y;
	y=a;
	return 100;
end
}
integer main()
{
decl
	string u,v;
enddecl
begin
	read(u);
	read(v);
	write(swap(u,v));
	write(u);
	write(v);
	return 0;	
end
}
